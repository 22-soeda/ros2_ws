#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""片脚 FK/IK の寸法パラメータ。

``leg_ik.py`` が読む値はこのファイルにだけ置く。
**現状の値はすべて仮置き**で、CAD 確定後にここだけを書き換えれば
``leg_ik.py`` には一切手を入れなくてよい。

単位は長さ mm、角度 deg（``leg_ik`` 側で rad に変換する）。
記号は ``docs/脚IK導出.tex`` 表 1 に対応する。
"""

from __future__ import annotations

__all__ = [
    "L3", "L4", "L5", "L6", "A3_X", "A4_X", "B_THIGH",
    "HIP_X", "HIP_Y", "HIP_Z", "SIGMA", "P6",
    "AXIS_FLIP", "AXIS_FLIP_LEFT", "JOINT_LIMITS_DEG",
]

# ---------------------------------------------------------------------------
# リンク長（文書 表 1 の ℓ_k）                                    [mm]
# ---------------------------------------------------------------------------
L3 = 80.0     # 大腿の高さ成分: 股中心 o3 -> 膝軸 o4
L4 = 80.0     # 下腿の高さ成分: 膝軸 o4 -> 足首ピッチ軸 o5
L5 = 6.0      # 足首 2 軸の高さオフセット o5 -> o6（数 mm。ℓ4 > ℓ5 が必要）
L6 = 25.0     # 足首ロール軸 o6 -> 足先基準点

# ---------------------------------------------------------------------------
# リンクの x 成分
# ---------------------------------------------------------------------------
# p3 = (A3_X, B_THIGH, -L3)   股中心 o3 -> 膝軸 o4   (Σ_3)
# p4 = (A4_X, 0,       -L4)   膝軸 o4   -> 足首軸 o5 (Σ_4)
#
# x は膝軸 (J4) の方向なので、o4 を膝軸に沿って滑らせても関節は変わらない。
# つまり A3_X と A4_X は和 a = A3_X + A4_X だけが幾何に効き、分け方は結果に
# 影響しない。CAD の実測値をそのまま入れてよい。
A3_X = 0.0
A4_X = 0.0

# ---------------------------------------------------------------------------
# その他のオフセット
# ---------------------------------------------------------------------------
#: 大腿の y オフセット（膝軸が股中心の真下から屈曲面内にずれる量）。
#: 0 でなくても ℓ3' = hypot(ℓ3, b)、θ4' = θ4 - atan2(b, ℓ3) で吸収できる（文書 §6）。
B_THIGH = 0.0

#: ボディ原点 -> 股中心 o3 のベクトル p0。右脚は +HIP_X、左脚は -HIP_X を使う。
#: TODO(CAD): x が左右で合っているか要確認。文書 §2 は x = ロール軸方向、
#:            膝が x まわりに曲がる（屈曲面 y-z = 前後面）と定めているので
#:            x = 左右・y = 前後 と読んでいる。CAD 座標を直接使うなら x <-> y。
HIP_X = 34.0
HIP_Y = 0.0
HIP_Z = -30.0

#: o6 -> 足先基準点 p6 (Σ_6)。None なら (0, 0, -L6) を使う。
#: 足裏の接地基準点を足首軸の真下から前後・左右にずらすならここで指定する
#: （p6 は z 成分以外を持ってよい。文書 §2.2）。
P6 = None

#: 膝の分岐 σ = ±1。arccos は θ4 と -θ4 を区別しないので機体で 1 つに固定する。
#: これは AXIS_FLIP を適用する前（文書の符号）での向きなので、
#: AXIS_FLIP["knee"] = 1 のときは ik() が返す θ4 の符号は σ と逆になる。
#: TODO(CAD): 機体の前方が +y か -y かを確認して決める（文書 §5.6「膝の分岐」）。
SIGMA = +1

# ---------------------------------------------------------------------------
# 各軸の回転方向
# ---------------------------------------------------------------------------
# 0 = 文書どおり（軸の正方向に対する右ねじが正）
# 1 = 逆（正負を入れ替える）
#
# 実機の軸の向きが文書の仮定と逆になっている関節をここで 1 にする。
# fk() に渡す角も ik() が返す角も、この符号を適用したあとの値になる。
# 幾何そのものは変わらないので、膝がどちら側に曲がるかは SIGMA が決める
# （AXIS_FLIP["knee"] を 1 にしても曲がる向きは変わらず、報告される符号だけ反転する）。
#
# TODO(実機): 1 軸ずつ正方向に少し動かして、文書の向きと合っているか確認する。
AXIS_FLIP = {
    "hip_pitch":   0,
    "hip_roll":    0,
    "hip_yaw":     0,
    "knee":        0,
    "ankle_pitch": 0,
    "ankle_roll":  0,
}

#: 左脚の回転方向。左右でサーボの向きが鏡像になっている場合はここに別の表を書く。
#: None なら AXIS_FLIP と同じものを使う。
AXIS_FLIP_LEFT = None

# ---------------------------------------------------------------------------
# 可動域（仮置き。文書 §8 の検算で使った範囲）                     [deg]
# ---------------------------------------------------------------------------
# ※ AXIS_FLIP を適用する前、つまり文書の符号での範囲。
#: knee は σ を掛ける前の大きさ。実際の θ4 は σ * この範囲 + atan2(b, ℓ3)。
#: TODO: サーボのリミット（config/servo_limits.yaml）と突き合わせて確定する。
JOINT_LIMITS_DEG = {
    "hip_pitch":   (-60.0, 60.0),
    "hip_roll":    (-60.0, 60.0),
    "hip_yaw":     (-60.0, 60.0),
    "knee":        (0.0, 150.0),
    "ankle_pitch": (-60.0, 60.0),
    "ankle_roll":  (-60.0, 60.0),
}
